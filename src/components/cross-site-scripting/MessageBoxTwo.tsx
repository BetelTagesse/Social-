
import React from 'react';
import { Box, Card, Grid, Paper, TextField, Typography } from '@mui/material';

import MessageBoxOne from './MessageBoxOne';

const MessageBoxTwo: React.FC = () => {
  return (
    <MessageBoxOne imageSrc = '/assets/vic-baking.png' altText='baking Forum' />

  );
};

export default MessageBoxTwo;
